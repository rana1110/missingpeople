package com.ohm.missingpeople.utils;

import android.app.Activity;
import android.content.Context;
import android.content.Intent;
import android.net.Uri;

import com.ohm.missingpeople.activity.HomeScreenActivity;

public class AppUtils {

    /*public static void openURl(Context context, String url) {
        Intent openUrl = new Intent(Intent.ACTION_VIEW, Uri.parse(url));
        openUrl.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        MissingPeopleApplication.getInstance().getApplicationContext().startActivity(openUrl);
    }

    public static void OpenNewScreen(Activity screenName) {
        Intent openIntent = new Intent(MissingPeopleApplication.getInstance().getApplicationContext(), screenName.getClass());
        openIntent.addFlags(Intent.FLAG_ACTIVITY_NEW_TASK);
        MissingPeopleApplication.getInstance().getApplicationContext().startActivity(openIntent);

    }*/
}
